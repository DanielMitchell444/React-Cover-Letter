import React from 'react'
import styles from '../App.module.css';

const Navigation = () => {
  return (
   <header className= {styles.stuff}>
    <h1 className= {styles.heading}>CV App</h1>
   </header>
  )
}

export default Navigation;